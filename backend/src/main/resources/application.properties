server.port=8000

# SQLite
spring.datasource.url=jdbc:sqlite:travelpilot.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Disable Hibernate (we use JDBC directly)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Jackson date format
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=Asia/Kolkata

# LLM Configuration (optional)
# travelpilot.llm.api-key=
# travelpilot.llm.api-url=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent
# travelpilot.llm.provider=gemini
